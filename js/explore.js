
// Create a function that takes in a string as an input
// Iterate over the input string from the last word
// Create an empty variable 
// Add each index to the empty variable
// Return the Variable 





function reverse(str){
	fav = "";
	for(i = str.length-1; i>=0;i--){
		fav += str[i];
		
	}
	return (fav);
}

var whisky = reverse("drake")
console.log(whisky)


if (5 > 4){
	console.log(whisky)
}